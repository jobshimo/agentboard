// Settings view placeholder — S10d implements workflows, MCP, attention, export.
import { en } from "../i18n/en";

export function Settings() {
  return (
    <div className="settings-page">
      <p className="muted">{en.placeholder_settings}</p>
    </div>
  );
}
